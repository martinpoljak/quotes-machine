<?php
	// Includes initialization routines
	include_once(($qm_path ? $qm_path : ($include_root ? $include_root : null)) . "libs/libraries.init.php");

	// Includes authorization routines
	include_once(($qm_path ? $qm_path : ($include_root ? $include_root : null)) . "libs/libraries.auth.php");
?>
<h1>Administration</h1>
